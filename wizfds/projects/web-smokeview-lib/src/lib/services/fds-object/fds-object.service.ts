import { Injectable } from '@angular/core';
import { forEach, find, lowerCase, toNumber } from 'lodash';
import { ISurf, IObst } from '../drawing/interfaces';
import { colors } from '../../consts/colors';

@Injectable({
  providedIn: 'root'
})
export class FdsObjectService {

  constructor() { }

}
