import { RgbInputDirective } from './rgb-input.directive';

describe('RgbInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RgbInputDirective();
    expect(directive).toBeTruthy();
  });
});
